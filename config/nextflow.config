params {
    datadir = "resources/fastq"
    outputdir = "output/"
    statsdir = "${params.outputdir}/statsFiles/"
    plotdir = "${params.outputdir}/plots/"
    threads = 8
}

process {
    shell = '/bin/bash -eo pipefail'
    env.PATH = "scripts/:scripts/Rscripts/:$PATH"
}
